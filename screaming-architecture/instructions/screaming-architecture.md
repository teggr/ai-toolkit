# Screaming Architecture Router

When architecture design, creation, or review is requested, invoke the screaming-architecture skill.

This file is a router only.
All architecture rules, workflow, and scoring are defined in the skill and its docs.

Examples:

- /screaming-architecture review this package tree
- /screaming-architecture propose a feature-first module layout
- /screaming-architecture refactor from layered to feature-first
